services:
  - type: web
    name: financial-nlp-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m nltk.downloader -d /tmp/nltk_data punkt punkt_tab
    startCommand: python -m nltk.downloader -d /tmp/nltk_data punkt punkt_tab && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: AIRTABLE_API_TOKEN
        sync: false
      - key: NLTK_DATA
        value: /tmp/nltk_data
